{
  "name": "@wahengchang2023/chainq",
  "version": "0.2.1",
  "type": "module",
  "description": "Multi-step prompt chains on local AI CLIs, defined in one YAML file — edit and run them on the same canvas, on the CLI login you already have.",
  "keywords": [
    "ai",
    "llm",
    "prompt",
    "prompt-chaining",
    "workflow",
    "ai-workflow",
    "cli",
    "claude",
    "claude-code",
    "codex",
    "agent-skill",
    "local-first",
    "visual-editor",
    "yaml",
    "dag"
  ],
  "license": "MIT",
  "author": "wahengchang",
  "homepage": "https://wahengchang.github.io/chainq/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wahengchang/chainq.git"
  },
  "bugs": {
    "url": "https://github.com/wahengchang/chainq/issues"
  },
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "chainq": "./dist/cli/index.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "skills",
    "!**/._*",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && mkdir -p dist/web/ui && cp src/web/app.html dist/web/app.html && cp src/web/ui/*.js dist/web/ui/ && find dist -name '._*' -delete",
    "prepack": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "bun src/cli/index.ts",
    "ui": "bun src/cli/index.ts ui",
    "typecheck": "tsc --noEmit && npm run typecheck:ui",
    "typecheck:ui": "tsc -p src/web/ui/tsconfig.json",
    "e2e": "vitest run --config vitest.e2e.config.ts",
    "e2e:cli": "vitest run --config vitest.e2e.config.ts",
    "e2e:demo": "tsx e2eCli/demo.ts",
    "e2e:ui": "playwright test --ui",
    "e2e:ui:headless": "playwright test",
    "e2e:ui:headed": "playwright test --headed",
    "e2e:ui:demo": "SLOWMO=${SLOWMO:-900} playwright test --headed -g editor",
    "e2e:ui:debug": "playwright test --debug -g editor",
    "docs:links": "node scripts/check-doc-links.mjs"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
