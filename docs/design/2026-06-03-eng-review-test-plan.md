# Test Plan — chainq init + E2E verification
Generated by /plan-eng-review on 2026-06-03

> **Historical snapshot — records the plan, not shipped behavior.** There is no
> `fake`/offline profile; `ai`-node E2E uses the real `claude -p` (gated). `chainq run`
> re-runs everything by default (`--cache` reuses). See [README](../../README.md) and [docs/cli/](../cli/).

## Critical Paths
- chainq init → chainq run flow.yaml --profile fake → every node runs (the new-project journey)
- chainq init refuses to clobber an existing flow.yaml (exit 1) unless --force

## Key workflows to verify (E2E, offline via fake=cat)
- init-scaffold · init-refuse · linear-cache (cold/cached/edit) · cmd-inputs (cwd+cacheable)
- pin-scratch (isolation) · multi-input (from-order) · validate-fail (broken flow rejected)

## Edge cases
- init in non-empty dir; init in nested new dir; --force overwrite
- spawn CLI from temp cwd → must use absolute tsx binary (not npx)
